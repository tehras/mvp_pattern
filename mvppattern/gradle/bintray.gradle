ext {
    tagVersion = (System.env.TRAVIS_TAG != null && !System.env.TRAVIS_TAG.isEmpty()) ? System.getenv().getOrDefault('TRAVIS_TAG', '0.0.1') : '0.0.1'

    bintrayRepo = 'maven'
    bintrayName = 'mvp_patterns'

    publishedGroupId = 'com.github.tehras'
    libraryName = 'MVPPatterns'
    artifact = 'mvp-patterns'

    libraryDescription = 'MVP Base Classes with Dagger'

    siteUrl = 'https://github.com/tehras/mvp_pattern'
    gitUrl = 'https://github.com/tehras/mvp_pattern.git'

    libraryVersion = "$tagVersion"

    developerId = 'tehras'
    developerName = 'Taras Koshkin'
    developerEmail = 'tkoshkin@gmail.com'

    licenseName = 'The Apache Software License, Version 2.0'
    licenseUrl = 'http://www.apache.org/licenses/LICENSE-2.0.txt'
    allLicenses = ["Apache-2.0"]
}
